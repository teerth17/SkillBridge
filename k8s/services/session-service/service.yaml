apiVersion: v1
kind: Service
metadata:
  name: session-service
  namespace: skillbridge
  labels:
    app: session-service
spec:
  type: ClusterIP
  selector:
    app: session-service
  ports:
    - name: http
      port: 4002
      targetPort: 4002
      protocol: TCP
